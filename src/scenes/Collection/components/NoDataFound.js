// @flow
import React from "react";

import Empty from "../../../components/Empty";

const NoDataFound = () => (
  <Empty>
    <p>There's no data</p>
    <p>All your created pages will be displayed here!</p>
  </Empty>
);

export default NoDataFound;
