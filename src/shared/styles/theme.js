const COLORS = {
  // Example: white: '#FFF';
};

const SPACING = {
  // Example: padding: 5px;
};

const base = {
  // base styling
};

export const lightTheme  = {
  text: 'red'
};

export const darkTheme  = {
  text: 'blue'
};

export default darkTheme;