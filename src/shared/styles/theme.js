const COLORS = {
  // Example: white: '#FFF';
};

const SPACING = {
  // Example: padding: 5px;
};

const base = {
  // base styling
};

export const lightTheme  = {
  text: 'black'
};

export const darkTheme  = {
  text: 'black'
};

export default darkTheme;